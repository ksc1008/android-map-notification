<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="ksc.campus.tech.kakao.map.presentation.viewmodels.SplashActivityViewModel"/>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".presentation.views.SplashScreenActivity">

        <ImageView
            android:id="@+id/alert_image"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:padding="30dp"
            android:src="@drawable/alert_circle_outline_icon"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.291" />

        <TextView
            app:layout_constraintTop_toBottomOf="@id/alert_image"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewModel.launchErrorMessage}"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>