<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="viewHolder"
            type="ksc.campus.tech.kakao.map.presentation.views.adapters.SearchKeywordAdapter.SearchKeywordViewHolder" />
        <variable
            name="keyword"
            type="String" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:onClick="@{()->viewHolder.onDeleteButtonClick()}"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginStart="5dp"
            android:foreground="@drawable/x_icon" />

        <TextView
            android:onClick="@{()->viewHolder.onKeywordClick()}"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="5dp"
            android:layout_weight="1"
            android:gravity="center_vertical|start"
            android:text="@{keyword}" />

    </LinearLayout>
</layout>